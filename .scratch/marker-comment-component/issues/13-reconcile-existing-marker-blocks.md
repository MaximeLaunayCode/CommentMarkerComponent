# 13 — Reconcile Existing Marker Comment Blocks

**What to build:** Make existing Marker Comment Blocks converge safely to the configured canonical form. A single valid block is updated and moved when needed, canonical content remains untouched, and ambiguous or malformed marker structures are reported without guessing or partially rewriting the affected file.

**Blocked by:** 11 — Render All Line-Comment Syntax Families; 12 — Render Safe HTML and CSS Block Comments.

**Status:** resolved

- [x] Exact syntax-aware matches for the currently configured begin and end labels identify one well-formed Marker Comment Block in every supported syntax family.
- [x] A valid block with a stale Managed Marker Body is replaced canonically, and a valid block in the wrong position is moved to the first safe content position without otherwise changing the file.
- [x] A canonical block already at the safe position produces no file or patch change.
- [x] Blocks using old sentinel labels remain ordinary content and a new block using the configured labels is inserted without migration.
- [x] Lone, reversed, nested, duplicate, and structurally invalid markers leave the affected file byte-for-byte unchanged and produce a processing error.
- [x] Processing continues after per-file errors, valid files contribute to a binary-capable partial patch, and error status `2` outranks enforcement or Report-Only Mode status.
- [x] The final summary and Changed Files, Excluded MR-Added Files, and Errors sections are complete, deterministically ordered, and safely escape non-printable paths.
- [x] Tests cover absent, canonical, stale, misplaced, relabeled, and every malformed block form across line and block syntax families, including repeated-run idempotency.

## Comments

> *This was generated by AI during implementation.*

Resolved by commit `9f2b8a4` (`Reconcile existing marker blocks`). The processor
now reconciles a single valid block, preserves malformed files while producing
partial patches for valid files, and remains idempotent across line and block
comment syntax families. The required standards and spec reviews completed with
no remaining actionable findings.
